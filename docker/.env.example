# Copy to .env next to docker-compose or export before `docker compose up`.
# Override the default JWT used by gateway/auth/trend.
JWT_SECRET=PRN232ASM_JWT_Secret_Key_2026_Change_In_Production_32chars
